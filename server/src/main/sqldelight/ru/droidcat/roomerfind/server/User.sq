CREATE TABLE user (
    id TEXT NOT NULL PRIMARY KEY,
    name TEXT DEFAULT NULL,
    email TEXT NOT NULL
);

addUser:
INSERT INTO user (id, email)
VALUES (:id, :email);

getUser:
SELECT *
FROM user
WHERE id = :userId;

setName:
UPDATE user
SET name = :name
WHERE id = :userId;
